# Render build wrapper (python runtime).
#
# The canonical repo (Fuhad-Lab/longhorizon-agent-platform) is PRIVATE and the
# Render GitHub App cannot read it, so the platform itself is installed at
# build time from git. GITHUB_TOKEN is a Render env var (never committed);
# pip expands ${GITHUB_TOKEN} in requirements lines.
git+https://x-access-token:${GITHUB_TOKEN}@github.com/Fuhad-Lab/longhorizon-agent-platform.git@main#egg=agent-platform

# 2026-09-11: trigger rebuild pulling 7237df3 (live failure-observability fixes)

# 2026-09-11b: hotfix — worker activity registry (m-3248414cf470)

# 2026-09-11c: ActivityError cause-chain evidence

# 2026-09-12: deploy — convex mirror + LSP gate + MCP tools + production law (2ac42bf)

# 2026-09-12b: deploy — escalate-alias fix + list_tables via /database/query + commentary name strip (d40e1f7)

# 2026-09-12c: deploy — the stall guard + checkpoint hardening

# 2026-09-12d: deploy — refusal-loop fix (arg normalization + tool-error law)

# 2026-09-12e: deploy — executor-scoped stall guard + degenerate-turn breaker

# 2026-09-12f: deploy — stall guard settles on productivity alone
# 2026-09-14: deploy — the web-first build law (exportPathMap killed every build) (358c8cb)
# 2026-09-14b: deploy — double-encode repair + pipe exit-code fix + 480s build headroom (57d1feb)
# 2026-09-14c: deploy — has_pkg gate fix + 480s bash ceiling (9ccfc29)
# 2026-09-14d: deploy — the use client directive guard (use client enforcement)
# 2026-09-14e: deploy — layout metadata strip + client layout law
# 2026-09-14f: deploy — the layout exception (server layout + client providers)
